[package]
name = "stalwart"
version = "0.1.0"
edition = "2018"

[dependencies]
encoding_rs = {version=">=0.8.28", optional=true}
serde = { version = "1.0", features = ["derive"], optional=true}
serde_yaml = "0.8"

[features]
default = ["multibytedecode", "serde"]
multibytedecode = ["encoding_rs"]
